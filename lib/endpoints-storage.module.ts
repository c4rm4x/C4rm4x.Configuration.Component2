import { NgModule, } from "@angular/core";
import { StorageService } from "./endpoints-storage.service";

@NgModule({
    imports: [],
    exports: [StorageService],
    declarations: [StorageService],
    providers: [],
})

export class EndpointsStorageModule { }